# Environments / IDE
.venv/
venv311/
venv/
__pycache__/
*.egg-info/
.idea/
.pytest_cache/

# Build & run artifacts
scratch/
*_workdir/
*.m4b

# Large media and model binaries (miscellaneous one-off projects)
*.onnx
voices-v*.bin
*.mp4
*.wav
*.mp3
*.jpg
*.png

# Downloaded test-corpus PDFs (provenance + results live in docs/06-corpus.md)
corpus/

# Research playground — isolated experiments (model downloads, training runs,
# sample audio). Heavy artifacts stay out of git; the *findings* are committed
# as docs/08-voice-training-spike.md. Nothing here touches the shipped pipeline.
playground/
*.pt
*.pth
*.safetensors
*.ckpt
